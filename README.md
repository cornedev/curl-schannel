# curl-schannel
A curl build that uses schannel as it's backend instead of openssl.
I added this to a github repository to make it easier for people to find a curl build with schannel, because it's not easily findable on the internet.
